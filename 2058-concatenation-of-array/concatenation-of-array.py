class Solution(object):
    def getConcatenation(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        list=[]
        list.extend(nums)
        list.extend(nums)
        #for i in range(len(nums)*2):

        return list

        